# Backend 2026

Estructura objetivo para migracion incremental:

- `app/` capa FastAPI + dominio IA (canonica).
- `db/bootstrap/sql/` scripts SQL versionados.
- `langgraph.json` descriptor de grafos.
